<!DOCTYPE html>
<html>
  <head>
    <title>게시판</title>
    <link rel="stylesheet" href="http://cdn.jsdelivr.net/font-nanum/1.0/nanumbarungothic/nanumbarungothic.css">
  </head>
  <style>
    table {
        font-family:'Nanum Barun Gothic';
        border-collapse: collapse;
        margin: 10px;
        text-align: center;
        font-size: 15px;
        margin: 40px;
    }

    table td, th {
        padding: 15px;
    }

    table td {
        background-color: #f0f0f0;
        border-bottom: 1px solid #aaa;
    }

    table th {
        background-color: #d9e3f0;
        border-bottom: 1px solid #aaa;
        height: 5px;
    }

    .title {
        font-size: 40px;
        color: #93aed2;
        text-align: center;
        font-family:'Nanum Barun Gothic';
        margin-top: 50px;
    }
  </style>

  <body>
  <center>
  <h1 class="title">게시판</h1>
  <table>
    <th style="width: 30px;">번호</th>
    <th style="width: 120px;">제목</th>
    <th>작성자</th>
    <th>날짜</th>
    <!-- for문으로 게시글 번호, 제목, 작성자, 날짜 출력 -->
    <% for (const result of results) { %>
    <tr>
        <td style="font-weight: bold; font-size: 20px; width: 25px;"><%= result.NUM %></td>
        <td style="width: 100px;"><a href="/board/post?NUM=<%= result.NUM %>&EMAIL=<%= result.EMAIL %>"><%= result.TITLE %></td>
        <td><%= result.NAME %></td>
        <td><%= result.WDATE %></td>
    </tr>
    <% } %>
  </table>

  <a href="/board/write"><input type="button" class="button" value="글 작성"/></a>
  <a href="/board/delete"><input type="button" class="button" value="글 삭제"/></a>
  </center>
  </body>
</html>